install: install/python install/roles

install/python: @skip=Pipfile.lock
    pipenv install

install/roles:
    pipenv run ansible-galaxy install --roles-path=./roles/vendor -r roles/requirements.yml

lint:
    pipenv run ansible-lint roles/*
    pipenv run ansible-lint playbooks/**/*.yml
